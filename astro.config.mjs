// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Адрес сайта на GitHub Pages
  site: 'https://axstiz.github.io',
  // Базовый путь — имя репозитория
  base: '/Q_polar',
});
